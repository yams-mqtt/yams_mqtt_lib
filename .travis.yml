language: erlang
notifications:
  email: yet.another.mqtt.server@gmail.com
otp_release:
  - 18.3
before_script:
  - export PLT_FILENAME=include/
script:
  - rebar compile
  - rebar skip_deps=true eunit
  - dialyzer --no_check_plt --plt $PLT_FILENAME -r ebin/